// Canonical public contact address. UI should import this instead of hard-coding email addresses.
export const CONTACT_EMAIL = 'info@optiontrip.com';
export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;
